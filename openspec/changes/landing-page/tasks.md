# Tasks: Landing Page — Taller Gerardito

## Phase 1: Infrastructure / Foundation

- [x] 1.1 Update `src/styles/global.css` — Add Tailwind v4 `@theme` block with brand color tokens (`--color-brand-blue: #1e3a5f`, `--color-brand-orange: #f97316`, neutrals), typography tokens (`--font-family-sans: Inter`, `--text-hero`, `--text-section-title`), smooth scroll behavior (`scroll-behavior: smooth`), base body styles, and `@keyframes` for WhatsApp pulse animation
- [x] 1.2 Rewrite `src/layouts/Layout.astro` — Add `Props` interface (title, description), `lang="es"`, Google Fonts `<link>` with preconnect, SEO meta tags (title, description, canonical, keywords), Open Graph tags (`og:title`, `og:description`, `og:type`, `og:url`, `og:locale="es_HN"`, `og:image`), Twitter card meta, Schema.org `AutoRepair` JSON-LD block with business data (name, address, phone, hours, geo, founder), GSAP CDN `<script defer>` tags (core + ScrollTrigger), and GSAP initialization script with `prefers-reduced-motion` guard, hero entrance animations, `[data-animate='fade-up']` ScrollTrigger, and `[data-animate='stagger']` children stagger
- [x] 1.3 Delete boilerplate files — Remove `src/components/Welcome.astro`, `src/assets/astro.svg`, `src/assets/background.svg`
- [x] 1.4 Create placeholder `public/og-image.svg` — Generated SVG Open Graph placeholder image (1200x630) with brand colors and business name

## Phase 2: Core Components — Navigation & Hero

- [x] 2.1 Create `src/components/Navbar.astro` — Sticky navbar (`fixed top-0 w-full z-50`) with "Taller Gerardito" logo text, navigation links (Inicio `#inicio`, Servicios `#servicios`, Nosotros `#nosotros`, Equipo `#equipo`, Contacto `#contacto`) with smooth scroll behavior, orange WhatsApp CTA button linking to `https://wa.me/50489638660`, mobile hamburger menu toggle via `<script>` (vanilla JS class toggle), responsive layout (hamburger < `lg:`, horizontal links >= `lg:`), background opacity change on scroll
- [x] 2.2 Create `src/components/Hero.astro` — Full-viewport hero section with `id="inicio"`, business name "Taller Gerardito" as `<h1>` with `data-animate="hero-title"`, slogan "Con la mejor preparación en sistemas de encendido electrónico" with `data-animate="hero-subtitle"`, location "San Manuel, Cortés — Honduras", dual CTA buttons with `data-animate="hero-cta"` (primary: WhatsApp `wa.me/50489638660`, secondary: scroll to `#servicios`), brand-blue gradient background, responsive layout (stacked mobile, side-by-side desktop)
- [x] 2.3 Create `src/components/WhatsAppButton.astro` — Fixed-position button (`fixed bottom-6 right-6 z-50`), WhatsApp SVG icon, link to `https://wa.me/50489638660` with `target="_blank"` and `rel="noopener"`, green background (`#25D366`), pulse animation via CSS keyframe class, `aria-label="Contáctanos por WhatsApp"`, adequate spacing from edges on mobile (not overlapping content)

## Phase 3: Core Components — Content Sections

- [x] 3.1 Create `src/components/Services.astro` — Section with `id="servicios"`, heading "Nuestros Servicios", 6 service cards in `data-animate="stagger"` container: Mecánica General, Diagnóstico Electrónico (motor gasolina y diésel), Transmisiones Automáticas y Mecánicas, Electricidad Automotriz, Electrónica Avanzada, Aire Acondicionado. Each card with SVG icon placeholder, title, brief description. Grid layout: 1 col mobile, 2 cols `md:`, 3 cols `lg:`
- [x] 3.2 Create `src/components/About.astro` — Section with `id="nosotros"`, heading "Nuestra Historia" with `data-animate="fade-up"`, content from tallerInfo.txt §1.7: 10+ years of experience, founder Gerardo García, evolution from general mechanic to electronic diagnostics specialist, mission statement from §1.2, mention of 3-person team. Alternating background (`bg-neutral-100`). Layout: stacked mobile, 2-column text+visual desktop
- [x] 3.3 Create `src/components/Equipment.astro` — Section with `id="equipo"`, heading "Nuestro Equipo de Diagnóstico", 4 equipment cards in `data-animate="stagger"` container: Escáner Automotriz Multimarca (lectura OBD), Osciloscopio (señales eléctricas en tiempo real), Cámara Endoscópica (inspección interna sin desarmar), Banco de Pruebas (inyectores, alternadores, componentes eléctricos). Each with icon placeholder, name, description. Grid: 1 col mobile, 2 cols `md:`, 4 cols `lg:`
- [x] 3.4 Create `src/components/WhyUs.astro` — Section with `id="por-que"`, heading "¿Por Qué Elegirnos?", 4 advantage cards in `data-animate="stagger"` container: Certeza Diagnóstica (elimina prueba y error), Garantía Formal (política documentada), Precio Accesible (competitivo vs SPS), Tecnología Especializada (equipo de última generación). Each with icon, title, description. Grid: 1 col mobile, 2 cols `md:`, 4 cols `lg:`
- [x] 3.5 Create `src/components/Brands.astro` — Section with `id="marcas"`, heading "Marcas que Atendemos", brand grid in `data-animate="stagger"` container with text-based brand badges for: Toyota, Nissan, Mazda, Honda, Mitsubishi, Hyundai, Jeep, Ford, Chevrolet, plus "y más" indicator. Grid: 2 cols mobile, 3 cols `md:`, 5 cols `lg:`. Each brand displayed as styled badge/card with brand name
- [x] 3.6 Create `src/components/Testimonials.astro` — Section with `id="testimonios"`, heading "Lo Que Dicen Nuestros Clientes", 3 placeholder testimonial cards in `data-animate="stagger"` container, each with: placeholder name, star rating visual (5 stars), placeholder review text. CTA prompt encouraging Google Maps reviews. Grid: 1 col mobile, 2 cols `md:`, 3 cols `lg:`
- [x] 3.7 Create `src/components/Contact.astro` — Section with `id="contacto"`, heading "Contáctanos", display: phone "8963-8660" as clickable `tel:+50489638660`, WhatsApp link to `https://wa.me/50489638660`, email `mailto:gerarnoe21@gmail.com`, address "San Manuel, Cortés, 2 cuadras arriba del Carwash Mendoza", business hours "Lunes a Sábado, 8:00 a.m. a 5:00 p.m.". Each contact method with appropriate icon. Layout: stacked mobile, 2 columns `md:`
- [x] 3.8 Create `src/components/Map.astro` — Section with Google Maps `<iframe>` for San Manuel, Cortés location, `loading="lazy"` attribute, responsive width (`w-full`), fixed height (~400px), `border-0`, title attribute for accessibility. Wrapped in container with optional IntersectionObserver for deferred loading
- [x] 3.9 Create `src/components/Footer.astro` — Footer with `<footer>` semantic element, "Taller Gerardito" branding, copyright with current year (dynamic via Astro), quick links (Servicios, Nosotros, Contacto) with smooth scroll, placeholder social media icons (Facebook, Instagram), brand-blue-dark background with white text

## Phase 4: Page Composition & Integration

- [x] 4.1 Rewrite `src/pages/index.astro` — Import all components (Navbar, Hero, Services, About, Equipment, WhyUs, Brands, Testimonials, Contact, Map, Footer, WhatsAppButton), compose inside `<Layout>` wrapper with appropriate title and description props, wrap content sections in `<main>` semantic element, ensure correct section order per specs §1

## Phase 5: Testing & Verification

- [ ] 5.1 Run `astro build` — Verify build completes with exit code 0, no TypeScript errors, no template errors (specs §19.1)
- [ ] 5.2 Visual check at 3 breakpoints — Open dev server, verify all 10 sections render at 375px (mobile), 768px (tablet), 1280px (desktop). Check grid columns adapt per design responsive matrix (specs §15)
- [ ] 5.3 Verify GSAP animations — Check hero entrance plays on load (title, subtitle, CTAs within 2 seconds), scroll through page and confirm ScrollTrigger fires for each section at ~85% viewport, verify animations play only once (specs §14)
- [ ] 5.4 Verify `prefers-reduced-motion` — Toggle reduced motion in OS/browser, reload page, confirm no GSAP animations play and all content is immediately visible (specs §14.2)
- [ ] 5.5 Verify all links — Click WhatsApp button (opens `wa.me/50489638660`), click phone number (opens tel:), click email (opens mailto:), click all navbar links (smooth scroll to correct section), verify footer quick links work (specs §13, §10, §2)
- [ ] 5.6 Verify SEO — View page source: check `<title>` contains "Taller Gerardito", `<meta description>` present, `lang="es"` on `<html>`, OG tags present, JSON-LD block present with `@type: AutoRepair`, validate JSON-LD syntax (specs §16)
- [ ] 5.7 Verify content accuracy — Cross-reference displayed content against `tallerInfo.txt`: phone 8963-8660, email gerarnoe21@gmail.com, address San Manuel Cortés, hours Lunes-Sábado 8am-5pm, all 6 services, all 4 equipment items, all 9+ brands (specs §20)
- [ ] 5.8 Verify accessibility — Check semantic HTML (`<header>`, `<main>`, `<section>`, `<footer>`), heading hierarchy (h1 in hero, h2 per section), keyboard tab navigation reaches all links/buttons with visible focus, decorative icons have `aria-hidden="true"`, informative icons have alt text (specs §18)
